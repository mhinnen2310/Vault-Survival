package com.vaultsurvival.plugin.breach;

/** Stable extension point for breach workflows; district treasuries remain locked until explicitly implemented. */
public enum VaultTargetType {
    PLAYER_VAULT,
    DISTRICT_TREASURY
}
